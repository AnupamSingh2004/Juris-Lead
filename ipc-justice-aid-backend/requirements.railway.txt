# Core Django and API
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-oauth-toolkit==1.7.1
django-allauth==0.57.0
djangorestframework-simplejwt==5.3.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Redis (if needed)
redis==5.0.1
django-redis==5.4.0

# Configuration
python-decouple==3.8

# Google Auth
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# HTTP requests
requests==2.31.0

# Django extensions
django-extensions==3.2.3
django-ratelimit==4.1.0

# Security
cryptography==41.0.7
pyjwt==2.8.0

# Image processing (basic)
Pillow==10.1.0

# Celery for background tasks
celery==5.3.4
django-celery-beat==2.5.0
django-celery-results==2.5.1

# PDF generation (lightweight option)
reportlab==4.0.7

# OCR (if Railway supports it, otherwise use API)
# pytesseract==0.3.10
# opencv-python==4.8.1.78
# numpy==1.24.3

# AI/ML models (API-based, no local models)
huggingface-hub==0.19.4

# Payment processing
razorpay==1.3.0
stripe==7.8.0

# Production
gunicorn==21.2.0
whitenoise==6.6.0

# Monitoring
sentry-sdk[django]==1.38.0
